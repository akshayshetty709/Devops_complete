---
- name: Install and Configure Jenkins
  hosts: jenkins_servers
  become: true
  vars:
    jenkins_port: 8080
    jenkins_java_opts: "-Djenkins.install.runSetupWizard=false"
    java_package: temurin-17-jdk  # Eclipse Temurin (OpenJDK 17)

  tasks:

    # ── Java ──────────────────────────────────────────────────────────────────

    - name: Install required packages
      ansible.builtin.package:
        name:
          - curl
          - gnupg
          - apt-transport-https
          - ca-certificates
        state: present
      when: ansible_os_family == "Debian"

    - name: Add Adoptium GPG key (Debian/Ubuntu)
      ansible.builtin.apt_key:
        url: https://packages.adoptium.net/artifactory/api/gpg/key/public
        state: present
      when: ansible_os_family == "Debian"

    - name: Add Adoptium repository (Debian/Ubuntu)
      ansible.builtin.apt_repository:
        repo: "deb https://packages.adoptium.net/artifactory/deb {{ ansible_distribution_release }} main"
        state: present
        filename: adoptium
      when: ansible_os_family == "Debian"

    - name: Install Java (Debian/Ubuntu)
      ansible.builtin.apt:
        name: "{{ java_package }}"
        state: present
        update_cache: true
      when: ansible_os_family == "Debian"

    - name: Install Java (RedHat/CentOS)
      ansible.builtin.yum:
        name: java-17-openjdk-devel
        state: present
      when: ansible_os_family == "RedHat"

    # ── Jenkins ───────────────────────────────────────────────────────────────

    - name: Add Jenkins GPG key (Debian/Ubuntu)
      ansible.builtin.apt_key:
        url: https://pkg.jenkins.io/debian-stable/jenkins.io-2023.key
        state: present
      when: ansible_os_family == "Debian"

    - name: Add Jenkins repository (Debian/Ubuntu)
      ansible.builtin.apt_repository:
        repo: "deb https://pkg.jenkins.io/debian-stable binary/"
        state: present
        filename: jenkins
      when: ansible_os_family == "Debian"

    - name: Add Jenkins repository (RedHat/CentOS)
      ansible.builtin.get_url:
        url: https://pkg.jenkins.io/redhat-stable/jenkins.repo
        dest: /etc/yum.repos.d/jenkins.repo
      when: ansible_os_family == "RedHat"

    - name: Import Jenkins GPG key (RedHat/CentOS)
      ansible.builtin.rpm_key:
        key: https://pkg.jenkins.io/redhat-stable/jenkins.io-2023.key
        state: present
      when: ansible_os_family == "RedHat"

    - name: Install Jenkins (Debian/Ubuntu)
      ansible.builtin.apt:
        name: jenkins
        state: present
        update_cache: true
      when: ansible_os_family == "Debian"

    - name: Install Jenkins (RedHat/CentOS)
      ansible.builtin.yum:
        name: jenkins
        state: present
      when: ansible_os_family == "RedHat"

    # ── Configuration ─────────────────────────────────────────────────────────

    - name: Configure Jenkins port
      ansible.builtin.lineinfile:
        path: /etc/default/jenkins
        regexp: "^HTTP_PORT="
        line: "HTTP_PORT={{ jenkins_port }}"
        create: true
      notify: Restart Jenkins
      when: ansible_os_family == "Debian"

    - name: Configure Jenkins port (RedHat/CentOS)
      ansible.builtin.lineinfile:
        path: /etc/sysconfig/jenkins
        regexp: "^JENKINS_PORT="
        line: 'JENKINS_PORT="{{ jenkins_port }}"'
        create: true
      notify: Restart Jenkins
      when: ansible_os_family == "RedHat"

    - name: Set Jenkins Java options
      ansible.builtin.lineinfile:
        path: /etc/default/jenkins
        regexp: "^JAVA_ARGS="
        line: 'JAVA_ARGS="{{ jenkins_java_opts }}"'
        create: true
      notify: Restart Jenkins
      when: ansible_os_family == "Debian"

    # ── Firewall ──────────────────────────────────────────────────────────────

    - name: Allow Jenkins port through UFW (Debian/Ubuntu)
      community.general.ufw:
        rule: allow
        port: "{{ jenkins_port }}"
        proto: tcp
      when:
        - ansible_os_family == "Debian"
        - ansible_facts.packages['ufw'] is defined

    - name: Allow Jenkins port through firewalld (RedHat/CentOS)
      ansible.posix.firewalld:
        port: "{{ jenkins_port }}/tcp"
        permanent: true
        state: enabled
        immediate: true
      when:
        - ansible_os_family == "RedHat"
        - ansible_facts.services['firewalld.service'] is defined

    # ── Service ───────────────────────────────────────────────────────────────

    - name: Enable and start Jenkins service
      ansible.builtin.systemd:
        name: jenkins
        enabled: true
        state: started
        daemon_reload: true

    - name: Wait for Jenkins to be ready
      ansible.builtin.uri:
        url: "http://localhost:{{ jenkins_port }}/login"
        status_code: 200
      register: jenkins_response
      retries: 15
      delay: 10
      until: jenkins_response.status == 200

    # ── Initial admin password ────────────────────────────────────────────────

    - name: Retrieve initial admin password
      ansible.builtin.slurp:
        src: /var/lib/jenkins/secrets/initialAdminPassword
      register: jenkins_admin_password
      ignore_errors: true

    - name: Display initial admin password
      ansible.builtin.debug:
        msg: >
          Jenkins is running at http://{{ ansible_host }}:{{ jenkins_port }}
          Initial admin password: {{ jenkins_admin_password.content | b64decode | trim }}
      when: jenkins_admin_password is not failed

  # ── Handlers ─────────────────────────────────────────────────────────────────

  handlers:
    - name: Restart Jenkins
      ansible.builtin.systemd:
        name: jenkins
        state: restarted
        daemon_reload: true
